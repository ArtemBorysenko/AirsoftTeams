const express = require("express")const cntrl = require("../controllers/auth")const mail = require("../controllers/mailer")const socketNtfc = require("../controllers/socketNotifications")const router = express.Router()const config = require("../config")const {registerValidation} = require("../utils/validations/index")const {validationResult} = require("express-validator")router.post("/", registerValidation, async (req, res) => {    const errors = validationResult(req)    if (!errors.isEmpty()) {        return res.status(422).json({errors: errors.array()})    } else {        const error = await cntrl.registration(req, res)        if (error instanceof Error) {            res.status(400).json(error.message)        } else {            await socketNtfc.ntfcReg(req)            if (req.body.user_role === "Manager") {                await mail.mailer({                    from: '"Tetta App" <artemborysenco@gmail.com>',                    to: config.mailAdmin,                    subject: `Manager ${req.body.username} is registrated`,                    text: "This is the email sent through Gmail SMTP Server.",                })            }            res.status(200).json(error)        }    }})// сделал TODO функция в ней валидация// registration ntfcReg//  mailer { даныя собощения }module.exports = router